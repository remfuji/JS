// stampare i numeri da 0 a 10 orizzontalmente
for (let vertical = 0; vertical < 3; vertical++) {
    let o = ""
    for(let count = 0; count < 10; count++ ) {
        o = o + count
    }
    console.log(o)
}